<h2>ジャンル一覧・追加 </h2>
<%= form_with model: @genre do |f| %>
ジャンル名 <%= f.text_field :name, placeholder: "ジャンル名" %>
<%= f.submit "新規登録" %>
<% end %>

<% @genres.each do |genre| %>
<h6>ジャンル</h6>
<%= genre.name %>
<% end %>
