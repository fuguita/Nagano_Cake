<h3>配送先登録 / 一覧</h3>
<div class=form_group>
 <%= form_with model: @shipping do |f| %>
  <p>郵便番号(ハイフンなし ) <%= f.text_field :postal_code, placeholder: "0000" %></p>
  <p>住所 <%= f.text_field :address, placeholder: "東京都渋谷区代々木神園町0-0" %></p>
  <p>宛名 <%= f.text_field :name, placeholder: "令和道子" %></p>
  <p><%= f.submit '新規登録' %></p>
 <% end %>
</div>

<table class="table">
  <thead>
   <th>郵便番号</th>
   <th>住所</th>
   <th>宛名</th>
   <th></th>
 </thead>
 <tbody>
 <% @shippings.each do |shipping| %>
  <tr>
    <td><%= shipping.postal_code %></td>
    <td><%= shipping.address %></td>
    <td><%= shipping.name %></td>
    <td><%= link_to '編集する', edit_shipping_path(shipping.id) %> <%= link_to '削除する', shipping_path(shipping.id), method: :delete %> </td>
  </tr>
 <% end %>
 </tbody>
</table>