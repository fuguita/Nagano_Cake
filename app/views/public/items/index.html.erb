<div>
  <h3>商品一覧</h3>
  <span>全<%= @items.count %>件</span>
</div>
<% @items.each do |item| %>
<%= image_tag item.get_image(80,80) %>
<%= item.name %>
<%= (item.price*1.10).floor.to_s(:delimited) %>
<% end %>